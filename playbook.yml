- hosts: localhost
  tasks:
    - debug:
        msg: "I'm in group 'localhost'"

- hosts: onlyfoo
  tasks:
    - debug:
        msg: "I'm in group 'onlyfoo'"

- hosts: onlybar
  tasks:
    - debug:
        msg: "I'm in group 'onlybar'"
